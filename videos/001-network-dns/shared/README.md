# Network DNS: video-local library

`DnsChapterDivider` fixes the approved chapter-card direction for this video:
the shared `Route` composition, graphite palette, and an opaque full-screen
background. Individual animations contain only their chapter copy.

Chapter timing lives in `../anchors.json` as milliseconds. The four cards are
scheduled in `../timeline.json` and last four seconds each.
