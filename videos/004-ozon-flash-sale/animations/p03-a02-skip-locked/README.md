# Одна строка — одна единица

Композиция: `Video-004-ozon-flash-sale-p03-a02-skip-locked`.
Исходный интервал: **24:20.100–24:43.460**, длительность 23360 мс (701 кадр при 30 fps).
2560×1440, непрозрачная точечная доска Freeform, подсветка JetBrains.
Общий вход 900 мс и выход 1100 мс через CodeBoard.

## Сценарий

- 24:20.100 — ввод: одна строка на единицу товара.
- 24:21.740 — таблица reservation_units; в 24:24.720 звучит обещание сниппета на экране.
- 24:28.820 — SELECT id.
- 24:31.080 — WHERE item_id = 42.
- 24:32.940 — FOR UPDATE и появление трёх иллюстративных строк.
- 24:33.420 — обход занятой строки; 24:33.940 — LIMIT 1, выбор одной свободной.
- 24:35.940 — разные строки для конкурентных транзакций, итоговое удержание.

Запрос из flash-sale-script.md: SELECT id FROM reservation_units WHERE item_id = 42
FOR UPDATE SKIP LOCKED LIMIT 1. Комментарий напоминает о DELETE + INSERT в той же
транзакции; сцена не представляет SELECT как законченную резервацию.
Схема — пример выбора, не гарантия порядка строк без ORDER BY.
Пропускаются блокировки строк; не заявляется отсутствие любых ожиданий в СУБД.

## Код и проверка

Composition связывает общую доску, листинг, пояснения и локальную схему UnitRows.
Код и токены — sql.ts, тайминг — cues.ts и anchors.json. Общий SQL lexer вынесен
в shared/code/highlightSql.ts и используется также первой SQL-сценой.
Проверена структура репозитория: дополнительные глобальные компоненты не требуются;
схема строк остаётся локальной, общая доска и подсветка — внутри эпизода.

QA: out/qa/ozon-skip-locked. 17 кадров (вход, все подписи, промежуточный выбор,
финальное состояние, выход) и 3 кадра регрессии первой SQL-сцены.
Первый/последний кадр прозрачны; во время удержания alpha=255 на всей площади.
Проверены светлая/тёмная подложки, границы, стрелка, подписи и отсутствие наложений.
`task check` проходит; предупреждения только про отсутствующее аудио старых эпизодов.
Экспорт CapCut: `renders/004-ozon-flash-sale/clips/skip-locked-CapCut.mov`.
ProRes 4444, premultiplied alpha, 2560×1440, 30 fps, 701 кадр (23,366667 с),
без аудио; размер 1 157 648 300 байт. Начало на таймлайне 30 fps: `00:24:20:03`.
`task qa:capcut-alpha` проходит. Проверены 8 декодированных кадров на светлой
и тёмной подложках и края текста при 100% масштабе на входе и выходе.
На удержании alpha=255, первый и последний кадры alpha=0.
Отчёт и кадры: `out/qa/ozon-skip-locked-capcut/`.
